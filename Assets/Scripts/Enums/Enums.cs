public enum ItemType {Bat, Knife, Gun, Fist, Cloth, Head, Food, Drink, Material, Quest  }
public enum InputMode { GameOnly, UIOnly, GameAndUI }

public enum Stage { Apartment, Stationery, Convenient}

public enum ApartmentItems 
{ 
    shelf, 
    shoesCloset,
    Chiffonier,
    Closet,
    refrigerator,
    bookShelf,
    kitchen,
    backpack,
}

public enum EnemyType
{
    Light,
    Armored,
    HeavyArmored,
    Dangerous,

}
      
    